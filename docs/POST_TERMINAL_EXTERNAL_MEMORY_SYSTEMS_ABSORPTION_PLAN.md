# Post-Terminal External Memory Systems Absorption Plan / 后封顶外部记忆系统精华吸收方案

## 2. Plan Status

This is a docs-only plan.

It is based on the sealed `v6.16.0` Civilization Core Stable Kernel after:

- T1 Terminal Closure Pack merged;
- T2 Civilization Core Whitepaper merged;
- T3 Architecture Atlas merged;
- T4 Version Chronicle merged;
- T5 Boundary Constitution merged.

`v6_series_terminal_boundary` is terminal metadata only.

There is no `v6.17`.

This is a post-terminal candidate planning document. It does not activate runtime behavior, dependencies, adapters, dispatch, durable memory writes, Memory Graph mutation, MCP/API operation surfaces, or authorization/execution semantics.

This document does not implement `llm_wiki`, M-Flow, or GBrain.

## 3. Why This Exists

Civilization Core wants the strongest possible memory-system future without contaminating the sealed `v6.16.0` core.

External memory-system projects provide valuable forms:

- `llm_wiki` gives knowledge compilation form;
- M-Flow gives associative recall form;
- GBrain gives memory operations form.

Civilization Core provides governance, audit, authorization, boundary, and human sovereignty.

T5A exists before T6/T7/T8/T9/T10 to prevent future route drift. It records which external ideas can be borrowed as methodology now, which decisions must wait, which implementations must wait, and which imports must never happen.

Borrowing must happen as post-terminal methodology analysis. It must not reopen v6, change the Stable Kernel, or turn an external project into a hidden completion condition.

## 4. Non-Negotiable v6 Boundary

`v6.16.0` remains sealed.

There is no `v6.17`.

This plan does not change the package version.

This plan does not create a tag.

This plan does not create runtime behavior.

This plan does not add a dependency.

This plan does not add an adapter.

This plan does not add a durable memory writer.

This plan does not mutate the Memory Graph.

This plan does not create an MCP/API operation surface.

This plan does not authorize automatic execution.

This plan does not authorize automatic authorization.

This plan does not create self-evolution authority.

External projects are not v6 completion conditions.

## 5. Unified Interpretation

Civilization Core = governance kernel / memory constitution / boundary system.

`llm_wiki` = knowledge compiler.

M-Flow = associative recall engine.

GBrain = memory operations system.

Civilization Core should borrow forms, not import runtime.

Civilization Core should absorb methodology, not capability.

Civilization Core should preserve final authority, evidence review, approval boundaries, and non-execution semantics.

## 6. llm_wiki Knowledge Compilation Boundary

Borrowable essence:

- `raw_sources`;
- `compiled_wiki`;
- `purpose.md`;
- `schema.md`;
- `index.md`;
- `log.md`;
- `review_queue`;
- `lint_health`;
- `graph_insights`;
- raw source immutability;
- derived knowledge must remain candidate until reviewed.

Boundary rules:

- compiled wiki is not approved memory;
- generated page is not truth;
- review queue is not approval;
- lint health is not authorization;
- graph insight is not Memory Graph mutation;
- `llm_wiki` runtime is not imported;
- `llm_wiki` dependency is not added;
- no automatic ingest.

The borrowable lesson is knowledge compilation discipline: preserve raw inputs, compile derived pages, expose schema and purpose, keep logs, queue review, lint the corpus, and surface graph insight without treating generated knowledge as approved memory.

## 7. M-Flow Associative Recall Boundary

Borrowable essence:

- Episode;
- Facet;
- FacetPoint;
- Entity;
- semantic edge / `edge_text`;
- path-cost recall;
- bundle search;
- associative evidence path;
- one strong path can surface a recall candidate;
- recall candidate remains candidate only.

Boundary rules:

- associative recall is not durable memory adoption;
- path-cost evidence is not authorization;
- semantic edge is not Memory Graph mutation;
- M-Flow runtime is not imported;
- M-Flow dependency is not added;
- no `pip` installation of `mflow-ai`;
- no adapter;
- no actual vector retrieval activation;
- no actual graph retrieval activation.

The borrowable lesson is associative recall discipline: model episodes, facets, points, entities, semantic edges, and path costs as candidate evidence paths while preserving review before adoption.

## 8. GBrain Memory Operations Boundary

Borrowable essence:

- answer + citations + gaps;
- Brain / Source boundary;
- operation contract single source of truth;
- localOnly / remote / scope trust boundary;
- remote fail-closed model;
- doctor / health report;
- eval / replay / benchmark;
- thin client / team brain / operator system as productization reference.

Boundary rules:

- operation contract is not dispatch;
- doctor report is not remediation;
- eval pass is not authorization;
- GBrain runtime is not imported;
- GBrain dependency is not added;
- no MCP write/admin activation;
- no autopilot activation;
- no supervisor activation.

The borrowable lesson is memory operations discipline: answers should cite evidence, expose gaps, separate brain from source, define operation contracts, respect trust boundaries, fail closed on remote uncertainty, and evaluate through replayable checks.

## 9. Unified Candidate Architecture

Methodology-only candidate layers:

- Knowledge Compilation Candidate Layer;
- Associative Recall Candidate Layer;
- Memory Operations Candidate Layer;
- Governance Kernel Boundary.

Conceptual flow:

```text
Raw Source
→ Source Fingerprint
→ Compiled Knowledge Candidate
→ Purpose / Schema Boundary
→ Review Queue
→ Associative Recall Candidate
→ Citations + Gaps + Confidence
→ Human Review
→ Governed Decision
```

This flow is conceptual only.

No runtime is activated.

No write is authorized.

No execution is authorized.

## 10. Fifteen-Layer Mapping

This mapping is interpretive. It does not activate layers. External projects do not become Civilization Core identity.

| Layer | External-project essence mapping |
| --- | --- |
| 1. 星火记忆 | raw source entry, `llm_wiki` reference |
| 2. 星点记忆 | atomic fact / FacetPoint, M-Flow reference |
| 3. 星链记忆 | Episode / log / event chain, M-Flow + `llm_wiki` reference |
| 4. 星图记忆 | semantic graph / citation network / `edge_text`, all three references |
| 5. 星河记忆 | long-term compiled knowledge base, `llm_wiki` reference |
| 6. 星辰记忆 | knowledge and case sedimentation, `llm_wiki` reference |
| 7. 星域记忆 | Brain / Source domain partition, GBrain reference |
| 8. 星穹记忆 | audit / lint / doctor / review, GBrain + `llm_wiki` reference |
| 9. 星海记忆 | multi-source large-scale memory recall, M-Flow + GBrain reference |
| 10. 星界记忆 | cross-system memory coordination, GBrain reference |
| 11. 星枢记忆 | operation contract and recall routing candidate, GBrain + M-Flow reference |
| 12. 星律记忆 | rules, prohibitions, boundary constitution, Civilization Core reference |
| 13. 星魂记忆 | long-term continuity and personality boundary, Civilization Core reference |
| 14. 星宙记忆 | future cross-time / cross-system candidate operation boundary, GBrain reference only as candidate |
| 15. 星源记忆 | methodology-source boundary, all three external references only as methodology sources, not runtime |

## 11. What Can Be Borrowed Now

Borrow now:

- methodology;
- terminology;
- candidate schema ideas;
- boundary language;
- comparative analysis;
- future decision criteria;
- documentation structure;
- quality standards.

Now absorb methodology, not runtime.

Now establish candidate boundary, not capability dependency.

Now strengthen the future route, not the `v6.16.0` core.

现在吸收方法论，不吸收运行时。

现在建立候选边界，不建立能力依赖。

现在增强未来路线，不污染 v6.16.0 封顶内核。

## 12. What Must Wait Until T9

T9 must decide:

- whether to start v7;
- whether to design real knowledge compilation;
- whether to design real associative recall;
- whether to design real memory operations layer;
- whether to design MCP/API surface;
- whether to design Operator Console;
- whether to design benchmark / doctor system;
- go / no-go / defer / never matrix.

T9 is decision only.

T9 is not implementation.

T9 is not a v7 branch.

T9 is not a tag.

## 13. What Must Wait Until Future v7 / Productization

Future v7 or productization scope may consider:

- real ingest;
- real vector store;
- real graph store;
- real M-Flow bakeoff;
- real wiki compilation;
- real MCP server;
- real API surface;
- real doctor report;
- real eval benchmark;
- real UI / Operator Console;
- real product packaging.

Future implementation requires explicit new scope after T9.

Future implementation must not mutate `v6.16.0`.

Future implementation must keep human authority and governance boundaries.

## 14. What Must Never Be Imported

Never import:

- automatic durable memory write;
- automatic Memory Graph mutation;
- automatic authorization;
- automatic execution;
- automatic self-evolution;
- hidden external action;
- unsupervised MCP write/admin;
- runtime dependency hidden in docs;
- external project as completion condition;
- external project as Civilization Core identity;
- external project as active Layer 15 runtime;
- external project as Star-Source Memory runtime;
- autonomous authority;
- self-authorization;
- personhood;
- life;
- awakening;
- legal subject status;
- religious status.

These exclusions are permanent interpretation boundaries for this plan.

## 15. Productization Implication

`llm_wiki` gives knowledge compilation body.

M-Flow gives associative recall nervous system.

GBrain gives memory operations surface.

Civilization Core gives sovereignty, audit, authorization, and forbidden zones.

Future-only productization possibilities:

- personal brain;
- team memory system;
- enterprise knowledge governance system;
- AI Agent memory constitution layer;
- Operator Console;
- Memory Governance UI;
- Evidence / Review / Approval dashboard.

These are future possibilities only. They are not active implementation, not v6 continuation, and not a change to the sealed Stable Kernel.

## 16. Roadmap Placement

| Item | Status |
| --- | --- |
| T5 = Boundary Constitution | completed |
| T5A = External Memory Systems Absorption Plan | current |
| T6 = Operator Guide | next |
| T7 = Release Book | future |
| T8 = Handoff Package | future |
| T9 = v7 Pre-Design Decision | future / decision only |
| T10 = Productization Roadmap | future / outside v6 continuation |

T5A is inserted before T6 to prevent route drift.

T5A does not reopen v6.

T5A does not authorize v7.

T5A does not implement productization.

## 17. Final Absorption Rule

Civilization Core `v6.16.0` remains sealed.

External projects are post-terminal methodology candidates only.

Civilization Core should borrow form, not runtime.

Civilization Core should absorb methodology, not dependency.

Civilization Core should govern before operating.

Future work must remain outside v6 continuation.

llm_wiki gives Civilization Core the knowledge compilation body; M-Flow gives Civilization Core the associative recall nervous system; GBrain gives Civilization Core the memory operations surface; Civilization Core gives all of them sovereignty, audit, authorization, and forbidden zones.
