# Civilization Core Release Book / 文明之核最终发布书

## 2. Release Book Status

This is a docs-only release book.

It is based on the sealed `v6.16.0` Civilization Core Stable Kernel.

T1-T6 have merged as post-terminal documentation after `v6.16.0`:

| Item | Local git evidence |
| --- | --- |
| T1 Terminal Closure Pack | `docs: add civilization core terminal closure pack (#147)` |
| T2 Whitepaper | `docs: add civilization core whitepaper (#148)` |
| T3 Architecture Atlas | `docs: add civilization core architecture atlas (#149)` |
| T4 Version Chronicle | `docs: add civilization core version chronicle (#150)` |
| T5 Boundary Constitution | `docs: add civilization core boundary constitution (#151)` |
| T5A External Memory Systems Absorption Plan | `docs: add post-terminal external memory systems absorption plan (#152)` |
| T6 Operator Guide | `docs: add civilization core operator guide (#153)` |

`v6_series_terminal_boundary` is terminal metadata only.

There is no `v6.17`.

This document does not change the package version.

This document does not create a tag.

This document does not activate runtime behavior, dependencies, adapters, M-Flow behavior, `llm_wiki` behavior, GBrain behavior, MCP/API operation surfaces, durable memory writing, Memory Graph mutation, authorization semantics, or execution semantics.

This document records release facts and post-terminal documentation only.

This document does not create a release.

## 3. Purpose

T7 packages the final sealed `v6.16.0` identity.

T7 consolidates the final reading stack from T1 through T7.

T7 records what the release means and what it does not mean.

T7 prepares for T8 Handoff Package.

T7 does not authorize v7, productization, or implementation.

T7 is a release book, not release creation. It explains the sealed release identity, evidence boundaries, post-terminal document order, and stop conditions for future readers.

## 4. Final Release Identity

| Fact | Grounding |
| --- | --- |
| Package version remains `6.16.0`. | `pyproject.toml` contains `version = "6.16.0"`. |
| `v6.16.0` is the sealed Civilization Core Stable Kernel. | T1-T6 docs and local git log use this meaning. |
| The `v6.16.0` tag exists locally. | `git tag --list 'v6.*'` includes `v6.16.0`. |
| The `v6.16.0` tag anchors commit `1811063`. | Local `git log --oneline --decorate` shows `(tag: v6.16.0)` on `1811063`. |
| The sealed kernel PR is `#146`. | Local git log subject: `feat(governance): add civilization core stable kernel v6.16 (#146)`. |
| T1-T6 are post-terminal docs after `v6.16.0`. | Local git log records docs PRs `#147` through `#153` after `v6.16.0`. |

Release identity is evidence of what was sealed.

Release identity is not future permission.

The tag anchors release identity, not future mutation.

The T1-T6 documentation stack records, explains, constrains, and prepares the closed system. It does not alter the sealed kernel.

## 5. Final v6 Closure Sequence

The final local docs and git history identify this closure sequence:

| Version | Sealed meaning |
| --- | --- |
| `v6.13.0` | Civilization Core Stability Index |
| `v6.14.0` | Source Handoff Boundary |
| `v6.15.0` | Star-Source Closure Audit |
| `v6.16.0` | Civilization Core Stable Kernel |

`v6.16.0` ends the v6 line.

There is no `v6.17`.

`v6_series_terminal_boundary` is terminal metadata only.

There is no continuation through the terminal marker.

The terminal marker records closure after the Stable Kernel. It is not a release successor, branch target, implementation stage, or roadmap continuation inside v6.

## 6. Post-Terminal Documentation Stack

| Item | Role |
| --- | --- |
| T1 Terminal Closure Pack | Records the final closed state after `v6.16.0`, the terminal marker, the no-continuation boundary, and the intentionally unactivated surfaces. |
| T2 Whitepaper | Explains the governance thesis, system identity, fifteen-layer map, Stable Kernel interpretation, and non-overreach boundary. |
| T3 Architecture Atlas | Maps the closed architecture: layers, governance surfaces, version arc, evidence artifacts, execution boundaries, and post-terminal placement. |
| T4 Version Chronicle | Records the v1-v6 historical governance arc and the final `v6.13.0` through `v6.16.0` closure sequence. |
| T5 Boundary Constitution | Consolidates authority, memory, evidence, proposal, review, approval, execution, terminal marker, and non-overreach rules. |
| T5A External Memory Systems Absorption Plan | Places `llm_wiki`, M-Flow, and GBrain as post-terminal methodology candidates only. |
| T6 Operator Guide | Explains how a human operator should read, interpret, govern, and continue the documentation safely. |
| T7 Release Book | Packages release identity, evidence, boundaries, final reading order, non-goals, and remaining post-terminal roadmap. |

Post-terminal docs package, explain, constrain, hand off, decide, or plan.

Post-terminal docs do not modify `v6.16.0` Stable Kernel semantics.

Post-terminal docs do not create runtime capability.

## 7. Release Reading Order

Read the release stack in this order:

1. T1 Terminal Closure Pack
2. T2 Whitepaper
3. T3 Architecture Atlas
4. T4 Version Chronicle
5. T5 Boundary Constitution
6. T5A External Memory Systems Absorption Plan
7. T6 Operator Guide
8. T7 Release Book

Stricter boundary interpretation wins.

Release facts must be read through T5 Boundary Constitution and T6 Operator Guide.

If a release fact appears to imply future capability, reinterpret it through the stricter boundary rule: release evidence records what was sealed; it does not authorize future action.

## 8. What This Release Contains

This release contains:

- sealed governance kernel identity;
- final v6 boundary;
- stable kernel metadata;
- terminal marker metadata;
- evidence / proposal / review / approval / execution separation;
- human authority boundary;
- post-terminal documentation stack;
- external methodology candidates as candidates only.

The release contains a completed governance identity and documentation frame. It does not contain hidden implementation authority.

## 9. What This Release Does Not Contain

This release does not contain:

- no `v6.17`;
- no new release tag;
- no version bump;
- no runtime activation;
- no dependency activation;
- no adapter activation;
- no durable memory writer;
- no Memory Graph mutation;
- no MCP/API operation surface;
- no automatic authorization;
- no automatic execution;
- no autonomous authority;
- no active Layer 15 runtime;
- no active Star-Source Memory runtime;
- no external project implementation.

These exclusions are release boundaries. They must not be softened into future implementation permission.

## 10. External Methodology Candidate Release Note

`llm_wiki` = knowledge compiler methodology candidate.

M-Flow = associative recall methodology candidate.

GBrain = memory operations methodology candidate.

External projects are post-terminal methodology candidates only.

They are not implemented.

They are not dependencies.

They are not adapters.

They are not v6 completion conditions.

They are not Civilization Core identity.

现在吸收方法论，不吸收运行时。

现在建立候选边界，不建立能力依赖。

现在增强未来路线，不污染 v6.16.0 封顶内核。

External methodology may inform future decision material. It must not contaminate the sealed release identity, change the package, activate an operation surface, or become a hidden completion requirement.

## 11. Operator Safety Release Note

Use the T6 operator rules when reading this release:

- recall is not write;
- evidence is not truth by itself;
- audit is not authorization;
- review is not execution;
- approval request is not approval;
- adapter declaration is not dispatch;
- source mutation proposal is not source mutation;
- stable kernel metadata is not active runtime;
- terminal marker is not roadmap continuation;
- human sovereignty is non-transferable.

These rules govern tags, PRs, docs, tests, smokes, audits, release notes, candidate plans, and future roadmap text.

## 12. Evidence and Verification Note

Release facts must be grounded in git, docs, or checkpoints.

Tests, smokes, and audits are bounded evidence. They show that a stated check passed under a stated scope. They do not prove universal safety or future permission.

PR merge records repository change, not future authority.

Generated documents do not approve future work.

Tag anchors release identity, not future mutation.

Evidence does not authorize execution.

This release book therefore cites local package version, tags, git log subjects, and merged documentation. It does not invent unverified release meanings.

## 13. T7 Non-Goals

T7 is not:

- a release creation task;
- a new tag;
- a new package version;
- a v7 decision;
- a productization start;
- a runtime guide;
- a developer implementation guide;
- an external project adoption plan;
- a Memory Graph migration plan.

T7 records and packages the closed release state. It does not launch a new engineering line.

## 14. Remaining Roadmap After T7

| Item | Status after T7 |
| --- | --- |
| T8 Handoff Package | Next |
| T9 v7 Pre-Design Decision | Future / decision only |
| T10 Productization Roadmap | Future / outside v6 continuation |

T8 is handoff material, not development launch.

T9 is decision only, not v7 implementation.

T10 is roadmap only, not product implementation.

Future work remains outside v6 continuation.

T8 may package transfer material. T9 may decide whether a separate future line should exist. T10 may discuss productization outside the sealed v6 line. None of these items changes `v6.16.0`.

## 15. Release Book Stop Conditions

Stop immediately if any of these appear:

- source/test/script/pyproject changes;
- version changes from `6.16.0`;
- `uv.lock` appears;
- `v6.17` appears;
- dependency install instructions appear;
- forbidden runtime wording appears;
- adapter implementation appears;
- Memory Graph mutation appears;
- durable writer appears;
- authorization/execution semantics appear;
- external project becomes identity or completion condition;
- active Layer 15 runtime claim appears;
- active Star-Source Memory runtime claim appears;
- autonomous authority / self-authorization / personhood / life / awakening / legal subject / religious status claim appears.

Stopping means preserving evidence, classifying the issue, and returning to human review. It does not mean fixing the issue by implementation.

## 16. Final Release Statement

Civilization Core `v6.16.0` remains sealed.

T7 records release identity; it does not create a new release.

Post-terminal docs preserve boundary and prepare future handoff.

External projects remain methodology candidates only.

Future work must remain outside v6 continuation.

When uncertain, stop and classify as documentation / candidate / decision / roadmap only.

## 17. Appendix: Release Fact Table

| Fact | Value |
| --- | --- |
| Package version | `6.16.0` |
| Sealed kernel version | `v6.16.0` |
| Final v6 tag | `v6.16.0` |
| Final v6 release meaning | Civilization Core Stable Kernel |
| Final v6 tag commit | `1811063` |
| Final v6 PR reference visible in local git log | `#146` |
| Post-terminal docs completed through T7 | T1, T2, T3, T4, T5, T5A, T6, T7 |
| Next planned doc | T8 Handoff Package |
| Tag for T7 | None |
| v6 continuation after T7 | None |
